[package]
name = "zs-audit"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
description = "Construction, chaînage et signature des événements d'audit."

[lints]
workspace = true

[dependencies]
zs-crypto = { path = "../zs-crypto" }
thiserror = "2"

[dev-dependencies]
# Test seulement (tests/chain_vectors.rs) : parse le fichier de vecteurs figés
# tests/vectors/audit-seal-v1/chain.json. Aucun usage en code de production.
serde_json = "1"
