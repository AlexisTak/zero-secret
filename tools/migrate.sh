#!/usr/bin/env bash
# Applique les migrations PostgreSQL (quatre schémas cloisonnés) sur l'environnement local
# démarré par `make up`. Non implémenté — backlog L0.6.
set -euo pipefail

echo "migrate: NO-OP — aucune migration n'existe encore (backlog L0.6)." >&2
