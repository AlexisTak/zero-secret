//! `policy-engine` — PDP. Sans état, déterministe, rejouable hors ligne. Aucun appel réseau
//! pendant l'évaluation (règle absolue #5 du CLAUDE.md racine). Non implémenté — backlog L2+.

fn main() {
    eprintln!("policy-engine: non implémenté (backlog L2+)");
    std::process::exit(1);
}
