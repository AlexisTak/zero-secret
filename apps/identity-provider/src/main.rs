//! `identity-provider` — WebAuthn/FIDO2. Ne décide d'aucune autorisation (voir architecture).
//! Non implémenté — voir backlog L1.

fn main() {
    eprintln!("identity-provider: non implémenté (backlog L1)");
    std::process::exit(1);
}
