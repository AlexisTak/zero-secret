[package]
name = "some-app"
version = "0.1.0"
edition = "2024"

[dependencies]
# Violation : import crypto direct hors zs-crypto.
p256 = "0.13"
# Violation : motif étendu L1.1 (referent-crypto) — webauthn-rs implique OpenSSL en FFI.
webauthn-rs = "0.5"
